arg(R).
arg(M).
arg(A16).
arg(Alc).
arg(NS).
arg(NP).
arg(A18).
arg(NA).
arg(V).
arg(NM).

att(atR,M).
mem(attR,R).

att(atNS,M).
mem(atNS,NS).

att(atNPA18,M).
mem(atNPA18,NP).
mem(atNPA18,A18).

att(atA18,V).
mem(atA18,A18).

att(atA16M,M).
mem(atA16M,A16).

att(atA16Alc,Alc).
mem(atA16Alc,A16).

att(atNAA18,Alc).
mem(atNAA18,NA).
mem(atNAA18,A18).

att(atA18NM,Alc).
mem(atA18NM,A18).
mem(atA18NM,NM).