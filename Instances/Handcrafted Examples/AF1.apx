arg(r).
arg(h).
arg(j).

w(r,0.3).
w(h,0.6).
w(j,0.7).

att(r1,j).
mem(r1,r).
w(r1,0.5).

att(r2,j).
mem(r2,h).
w(r2,0.8).

att(r3,j).
mem(r3,r).
mem(r3,h).
w(r3,0.1).

supp(s1,j).
mem(s1,r).
mem(s1,h).
w(s1,0.5).

